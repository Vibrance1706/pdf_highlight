Using Node.js (if installed): If you have Node.js installed, you can use a simple package like http-server. First, install it globally:

npm install -g http-server


Then, navigate to your project directory and start the server:

http-server

This will serve your files on http://localhost:8080